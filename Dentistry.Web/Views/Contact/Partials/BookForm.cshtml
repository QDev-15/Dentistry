@using Dentistry.ViewModels.Catalog.Contacts
@using Dentisty.Web.Services
@inject ApplicationService _applicationService
@model BookFormVm
@{
    var branches = await _applicationService.GetBranches();
    var setting = await _applicationService.GetAppSetting();
    Model.branches = branches;
}

<form name="frmBook" id="frmBook" novalidate>
    <div class="row">
        <div class="col-sm-6">
            <div class="form-group" id="input-name">
                <input type="text" asp-for="@Model.contact.Name" class="form-control" placeholder="Họ tên khách hàng *">
                <span asp-validation-for="@Model.contact.Name" class="text-danger"></span>
            </div>
        </div>
        <div class="col-sm-6">
            <div class="form-group" id="input-phone">
                <input type="text" asp-for=@Model.contact.PhoneNumber class="form-control" placeholder="Số điện thoại liên hệ *">
                <span asp-validation-for="@Model.contact.PhoneNumber" class="text-danger"></span>
            </div>
        </div>
        <div class="col-sm-6">
            <div class="form-group" id="input-email">
                <input type="email" asp-for="@Model.contact.Email" class="form-control" placeholder="Email (để nhận xác nhận đặt lịch)">
                <span asp-validation-for="@Model.contact.Email" class="text-danger"></span>
            </div>
        </div>
        <div class="col-sm-6">
            <div class="form-group">
                <div class="input-group-datetimepicker">
                    @* FluentValidation không tự sinh data-val-required cho DateTime? (NotEmpty trên
                       kiểu ngày không được adapter phía client hỗ trợ) nên phải khai báo tay ở đây,
                       khớp đúng nội dung lỗi bên BookFormVmValidator.cs để 2 bên nhất quán. *@
                    <input type="text" asp-for="@Model.contact.TimeBook" class="form-control" placeholder="Ngày đặt lịch *"
                           data-val="true" data-val-required="Thời gian đặt lịch không được trống.">
                    <span class="input-group-text" id="calendarIcon">
                        <i class="fas fa-calendar-alt"></i>
                    </span>
                </div>
                <span asp-validation-for="@Model.contact.TimeBook" class="text-danger"></span>
            </div>
        </div>
        <div class="col-sm-6">
            <div class="form-group" id="input-location">
                @* Ô placeholder dùng value="" (không phải "0") để jQuery Validate coi đây là
                   "chưa chọn" một cách chuẩn (rule "required" mặc định của select) - value="0"
                   cũ vẫn được jQuery Validate tính là "đã có giá trị" nên không validate được.
                   Model binding value="" -> BranchesId=null, khớp đúng điều kiện Must() đang có
                   ở BookFormVmValidator.cs và điều kiện .Value > 0 ở ContactController.cs. *@
                <select asp-for="@Model.contact.BranchesId" class="form-control custom-dropdown"
                        data-val="true" data-val-required="Cơ sở khám không được trống.">
                    <option value="">Chọn cơ sở gần bạn *</option>
                    @{
                        foreach (var item in Model.branches)
                        {
                            <option value="@item.Id">@item.Name</option>
                        }
                    }
                </select>
                <span asp-validation-for="@Model.contact.BranchesId" class="text-danger"></span>
            </div>
        </div>
        <div class="col-sm-12">
            <div class="note">* Thông tin của bạn sẽ được bảo mật!</div>
            <div class="col-12 d-flex d- justify-content-sm-center justify-content-md-start">
                <div class="btnSuccess">
                    <button type="submit" class="btn_yellow submit">Đặt lịch</button>
                    <span class="open">
                        <span style="color: #404040;">Giờ làm việc: </span><br>
                        <span>@setting.StartWork đến @setting.EndWork</span>
                    </span>
                </div>
            </div>
            
        </div>
    </div>
</form>

